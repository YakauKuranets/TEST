# 🖥️ PLAYE PhotoLab - ДЕСКТОПНАЯ ВЕРСИЯ (ELECTRON)

**Дата:** 18 февраля 2026  
**Версия:** 3.0 FIXED (Desktop Edition)  
**Платформа:** Windows, macOS, Linux

---

## 🎯 ЧТО ЭТО?

Это **полноценное десктопное приложение** на базе Electron, которое включает:

- ✅ **Frontend** - веб-интерфейс (исправлен)
- ✅ **Backend** - Python сервер с PyTorch (FastAPI)
- ✅ **Electron** - нативная оболочка для десктопа
- ✅ **Автозапуск** - backend запускается автоматически
- ✅ **Нативные диалоги** - выбор файлов/папок через ОС
- ✅ **Загрузка моделей** - встроенный менеджер моделей

---

## 🚀 БЫСТРЫЙ СТАРТ (3 ШАГА)

### 1. Установить зависимости

```bash
cd PLAYE_Desktop_AI_stage3
npm install
```

**Требования:**
- Node.js 18+ (для Electron)
- Python 3.10+ (для backend)
- CUDA (опционально, для GPU)

### 2. Установить Python зависимости

```bash
cd backend
python -m venv .venv

# Windows:
.venv\Scripts\activate
pip install -r requirements.txt

# Linux/Mac:
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Запустить приложение

```bash
# Вернуться в корень проекта
cd ..

# Запустить в режиме разработки
npm start

# Или
npm run dev
```

**ВСЁ!** Приложение запустится автоматически! 🎉

---

## 🔧 ИСПРАВЛЕНИЯ ДЛЯ ДЕСКТОПНОЙ ВЕРСИИ

### ✅ Что было исправлено:

1. **Добавлен IPC handler `select-folder`**
   - Файл: `electron.js` (строка 191)
   - Позволяет выбирать папку через нативный диалог

2. **Добавлен IPC handler `select-file`**
   - Файл: `electron.js` (строка 201)
   - Позволяет выбирать .onnx файлы

3. **Обновлен preload.js**
   - Файл: `preload.js` (строки 157-159)
   - Добавлены `selectFolder()` и `selectFile()`

4. **Улучшена кнопка "Обзор..."**
   - Файл: `frontend/src/main.js` (строка 1384)
   - Теперь открывает нативный диалог выбора папки

5. **Автозагрузка пути к моделям**
   - При открытии настроек автоматически показывается путь
   - Работает только в Electron

---

## 📁 СТРУКТУРА ПРОЕКТА

```
PLAYE_Desktop_AI_stage3/
├── electron.js              ← Главный процесс Electron
├── preload.js               ← API между main и renderer
├── package.json             ← Конфигурация Node.js/Electron
│
├── frontend/                ← UI (исправленный)
│   ├── index.html
│   ├── styles.css
│   └── src/
│       ├── main.js
│       └── blueprints/
│
├── backend/                 ← Python backend (FastAPI)
│   ├── server.py
│   ├── requirements.txt
│   └── ...
│
├── models-data/             ← Папка для AI моделей
│
└── scripts/                 ← Утилиты
    ├── download-models.js   ← Загрузка моделей
    ├── check-updates.js     ← Проверка обновлений
    └── update-models.js     ← Обновление моделей
```

---

## 🎮 КАК ПОЛЬЗОВАТЬСЯ

### 1. Первый запуск

При первом запуске приложение:
- ✅ Автоматически запустит Python backend
- ✅ Создаст папку для моделей в `userData/models`
- ✅ Откроет главное окно

**Если backend не запустился:**
- Проверьте что Python установлен
- Проверьте что зависимости установлены
- Смотрите консоль для ошибок

### 2. Загрузка моделей

**Вариант A: Через UI (рекомендуется)**
1. Открыть вкладку "AI / Модели"
2. Найти "Модели (локальные)"
3. Нажать "Скачать/обновить"
4. Дождаться завершения загрузки

**Вариант B: Через настройки**
1. Нажать "⚙️ Настройки"
2. Выбрать .onnx файлы через кнопку "Обзор..."
3. Или указать папку с моделями
4. Нажать "💾 Сохранить"

### 3. Работа с файлами

**Загрузка видео:**
- Кнопка "Загрузить видео" - стандартный диалог ОС
- Поддержка drag & drop
- Любой размер файла ✅

**Выбор папки с моделями:**
- Настройки → "Обзор..." → нативный диалог ОС
- Путь сохраняется автоматически

---

## 🔌 ELECTRON API

### Доступные функции через `window.electronAPI`:

```javascript
// Проверка backend
const isRunning = await window.electronAPI.checkPythonBackend();

// Путь к моделям
const modelsPath = await window.electronAPI.getModelsPath();

// Выбор папки (НОВОЕ!)
const folder = await window.electronAPI.selectFolder();

// Выбор файла (НОВОЕ!)
const file = await window.electronAPI.selectFile({
  title: 'Выберите .onnx файл',
  filters: [
    { name: 'ONNX Models', extensions: ['onnx'] }
  ]
});

// Открыть папку с моделями
await window.electronAPI.openModelsFolder();

// Проверка обновлений
const updates = await window.electronAPI.checkModelUpdates();

// Обновить модели
const result = await window.electronAPI.updateModels();

// Показать диалог
await window.electronAPI.showDialog({
  type: 'info',
  title: 'Заголовок',
  message: 'Сообщение'
});

// Прогресс загрузки
const unsubscribe = window.electronAPI.onDownloadProgress((data) => {
  console.log(`${data.modelName}: ${data.progress}%`);
});

// AI функции (через backend)
const result = await window.electronAPI.enhanceFace(imageData);
const upscaled = await window.electronAPI.upscaleImage(imageData, 2);
const denoised = await window.electronAPI.denoiseImage(imageData, 'medium');
```

---

## 📦 СБОРКА ПРИЛОЖЕНИЯ

### Для Windows:

```bash
npm run build
```

Результат: `dist/PLAYE PhotoLab Setup.exe`

### Для macOS:

```bash
npm run build
```

Результат: `dist/PLAYE PhotoLab.dmg`

### Для Linux:

```bash
npm run build
```

Результат: `dist/PLAYE PhotoLab.AppImage`

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Python backend не запускается

**Причина:** Python не найден или нет зависимостей

**Решение:**
```bash
# Проверить Python
python --version  # должно быть 3.10+

# Установить зависимости
cd backend
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
pip install -r requirements.txt
```

### Проблема: Electron не запускается

**Причина:** Node.js устарел или нет зависимостей

**Решение:**
```bash
# Проверить Node.js
node --version  # должно быть 18+

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Модели не загружаются

**Причина:** Нет интернета или сервер недоступен

**Решение:**
1. Проверить интернет соединение
2. Скачать модели вручную:
   - GFPGAN: https://github.com/TencentARC/GFPGAN/releases
   - Real-ESRGAN: https://github.com/xinntao/Real-ESRGAN/releases
   - NAFNet: https://github.com/megvii-research/NAFNet/releases
3. Положить в папку моделей (см. через "Открыть папку")

### Проблема: Кнопка "Обзор..." не работает

**Причина:** Запущена веб-версия, а не Electron

**Решение:**
- Запустить через `npm start`, а не через HTTP сервер
- В веб-версии кнопка показывает сообщение о недоступности

### Проблема: CUDA не работает

**Причина:** CUDA не установлен или неправильная версия PyTorch

**Решение:**
```bash
# Проверить CUDA
nvidia-smi

# Переустановить PyTorch с CUDA
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

---

## 🔄 ОБНОВЛЕНИЕ МОДЕЛЕЙ

### Автоматическое обновление:

```bash
npm run update-models
```

### Ручное обновление:

1. Открыть приложение
2. Вкладка "AI / Модели"
3. Нажать "Проверить" - увидите доступные обновления
4. Нажать "Скачать/обновить"

### Через настройки:

1. Нажать "⚙️ Настройки"
2. Загрузить новые .onnx файлы
3. Нажать "💾 Сохранить"

---

## 📊 СИСТЕМНЫЕ ТРЕБОВАНИЯ

### Минимальные (CPU режим):

- **ОС:** Windows 10+, macOS 11+, Ubuntu 20.04+
- **CPU:** Intel Core i5 / AMD Ryzen 5
- **RAM:** 8 GB
- **HDD:** 5 GB свободного места
- **Python:** 3.10+
- **Node.js:** 18+

### Рекомендуемые (GPU режим):

- **ОС:** Windows 10+, Linux (Ubuntu 20.04+)
- **CPU:** Intel Core i7 / AMD Ryzen 7
- **RAM:** 16 GB
- **GPU:** NVIDIA RTX 2060+ (6 GB VRAM)
- **HDD:** 10 GB свободного места
- **CUDA:** 11.8+
- **Python:** 3.10+
- **Node.js:** 18+

**Примечание:** macOS не поддерживает CUDA, только CPU/MPS режим.

---

## 🎓 РАЗНИЦА С ВЕБ-ВЕРСИЕЙ

| Функция | Веб-версия | Десктоп (Electron) |
|---------|------------|-------------------|
| Загрузка файлов | `<input type="file">` | Нативный диалог ОС |
| Выбор папки | Недоступно | Нативный диалог ОС ✅ |
| Backend | Внешний сервер | Автозапуск встроенный ✅ |
| Модели | CDN/ручная загрузка | Менеджер моделей ✅ |
| Оффлайн работа | Ограничена | Полная поддержка ✅ |
| Производительность | Средняя | Высокая ✅ |
| Обновления | Вручную | Автоматические ✅ |

---

## 🛠️ РАЗРАБОТКА

### Режим разработки:

```bash
npm run dev
```

**Включает:**
- Автоматическое открытие DevTools
- Hot reload (перезагрузка при изменении)
- Подробные логи

### Запуск тестов:

```bash
# Backend тесты
npm run test:api-client
npm run test:job-params
npm run test:task-status

# CI smoke тест
npm run ci:smoke
```

### Структура IPC:

**Main Process → Renderer:**
```javascript
// electron.js
ipcMain.handle('my-function', async (event, arg) => {
  return result;
});
```

**Preload → Renderer:**
```javascript
// preload.js
contextBridge.exposeInMainWorld('electronAPI', {
  myFunction: (arg) => ipcRenderer.invoke('my-function', arg)
});
```

**Renderer:**
```javascript
// frontend/src/main.js
const result = await window.electronAPI.myFunction(arg);
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

Перед использованием убедитесь:

- [x] Node.js 18+ установлен
- [x] Python 3.10+ установлен
- [ ] `npm install` выполнен
- [ ] Backend зависимости установлены
- [ ] `npm start` запускает приложение
- [ ] Backend запустился (проверить консоль)
- [ ] UI открылось без ошибок
- [ ] Модели загружаются (через UI)

---

## 🎉 ВСЁ ГОТОВО!

**Десктопная версия исправлена и готова к использованию!**

**Новые возможности:**
- ✅ Выбор папок через нативный диалог
- ✅ Выбор .onnx файлов через диалог
- ✅ Автозагрузка пути к моделям
- ✅ Полная интеграция с ОС

**Можно запускать и пользоваться!** 🚀

---

**Версия:** 3.0 FIXED (Desktop)  
**Дата:** 18 февраля 2026  
**Исправлено:** Claude (Anthropic AI)
